void store(vector<string>s ){
    vector<string>::iterator it=s.begin();
   //printVector(s);
   string value,name,tag;
   tag=*it;
   //it++;
   cout<<tag<<endl;
   if(myCompare(*it,"value"))
   {
     //  it++;
   //value=*it;
   }
   else
   value=*it;
   
   /*if(myCompare(*it,"name"))
   name=*it;
   else 
   name=*it;*/
  // m.insert( make_pair(tag, make_pair(value,name)) );
   
}
